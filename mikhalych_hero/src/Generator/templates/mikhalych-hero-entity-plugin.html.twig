<?php

namespace Drupal\{{ machine_name }}\Plugin\MikhalychHero\Entity;

use Drupal\mikhalych_hero\Plugin\MikhalychHero\Entity\MikhalychHeroEntityPluginBase;

/**
 * Hero block for entity.
 *
 * @MikhalychHeroEntity(
 *   id = "{{ plugin_id }}",
 *   entity_type = "{{ entity_type }}",
 *   entity_bundle = {"*"}
 * )
 */
class {{ name }} extends MikhalychHeroEntityPluginBase {

{% if is_title %}
  /**
   * {@inheritdoc}
   */
  public function getHeroTitle() {
    return $this->getPageTitle();
  }

{% endif %}
{% if is_subtitle %}
  /**
   * {@inheritdoc}
   */
  public function getHeroSubtitle() {

  }

{% endif %}
{% if is_image %}
  /**
   * {@inheritdoc}
   */
  public function getHeroImage() {

  }

{% endif %}
{% if is_video %}
  /**
   * {@inheritdoc}
   */
  public function getHeroVideo() {

  }

{% endif %}
}
