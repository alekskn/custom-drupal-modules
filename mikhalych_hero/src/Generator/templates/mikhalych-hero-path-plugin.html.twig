<?php

namespace Drupal\{{ machine_name }}\Plugin\MikhalychHero\Path;

use Drupal\mikhalych_hero\Plugin\MikhalychHero\Path\MikhalychHeroPathPluginBase;

/**
 * Hero block for path.
 *
 * @MikhalychHeroPath(
 *   id = "{{ plugin_id }}",
 *   match_type = "{{ match_type }}",
 *   match_path = {"<front>"}
 * )
 */
class {{ name }} extends MikhalychHeroPathPluginBase {

{% if is_title %}
  /**
   * {@inheritdoc}
   */
  public function getHeroTitle() {
    return $this->getPageTitle();
  }

{% endif %}
{% if is_subtitle %}
  /**
   * {@inheritdoc}
   */
  public function getHeroSubtitle() {

  }

{% endif %}
{% if is_image %}
  /**
   * {@inheritdoc}
   */
  public function getHeroImage() {

  }

{% endif %}
{% if is_video %}
  /**
   * {@inheritdoc}
   */
  public function getHeroVideo() {

  }

{% endif %}
}
